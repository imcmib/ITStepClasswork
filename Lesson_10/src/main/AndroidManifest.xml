<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.itstep.android.classwork" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AutoCompleteTextViewActivity"/>
        <activity android:name=".activity.MultiAutoCompleteTextViewActivity"/>
        <activity android:name=".activity.ListViewActivity"/>
        <activity android:name=".activity.ListActivityActivity"/>
        <activity android:name=".activity.ItemsAdapterActivity"/>
        <activity android:name=".activity.ComponentsAdapterActivity"/>
        <activity android:name=".activity.ViewHolderAdapterActivity"/>
    </application>

</manifest>
